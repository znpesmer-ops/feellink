{
  "version": 2,
  "name": "feellink-backend",
  "buildCommand": "npm install --legacy-peer-deps && npx prisma generate && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
